-#    Talk Result item
-#

%li{:id => "result_#{result.id}", :class => "result_#{result.id}"}
  %span.r_label= result.class
  = link_to result.name, talk_path(result)
  - if result.track.present?
    %span= result.track.name