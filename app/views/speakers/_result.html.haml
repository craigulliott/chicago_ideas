-#    User Result item
-#

%li{:id => "result_#{result.id}", :class => "result_#{result.id}"}
  %span.r_label= result.class
  = link_to result.name, speaker_path(result)
  %span= result.title_abbreviated