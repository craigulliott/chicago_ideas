-#     Chicago Ideas Week
-#     _sponsors.html.haml
-#     01/15/2012

-# Sponsors Section
- if @sponsors.present?
  .box.right#sponsors
    %ul
      - @sponsors.each do |sponsor|
        %li.sponsor_item
          = link_to image_tag(sponsor.logo), "#{sponsor.url}"
