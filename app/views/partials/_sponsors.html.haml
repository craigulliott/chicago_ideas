-#     Chicago Ideas Week
-#     _sponsors.html.haml
-#     01/15/2012

-# Sponsors Section
- if @sponsors.present?
  .box.right#sponsors
    .nivoSlider
      - @sponsors.each do |sponsor|
        = link_to image_tag(sponsor.logo), "#{sponsor.url}", :class => "sponsor_item"
