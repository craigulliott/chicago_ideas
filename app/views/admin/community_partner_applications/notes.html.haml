= render 'member_controls'

= ssp_search_form Note

.table_section
  %h3 
    Notes
    = new_model_link Note, @community_partner_application
  .table_wrapper
    = render 'admin/notes/table.html.haml', :notes => @notes
  = paginate @notes