= render 'member_controls'

= ssp_search_form EventSpeaker

.table_section
  %h3 
    Event Speaker
    = new_model_link EventSpeaker, @event
  .table_wrapper
    = render 'admin/event_speakers/table.html.haml', :event_speakers => @event_speakers
  = paginate @event_speakers
