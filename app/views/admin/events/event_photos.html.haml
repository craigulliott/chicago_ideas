= render 'member_controls'

= ssp_search_form EventPhoto

.table_section
  %h3 
    Event Photos
    = new_model_link EventPhoto, @event
  .table_wrapper
    = render 'admin/event_photos/table.html.haml', :event_photos => @event_photos
  = paginate @event_photos