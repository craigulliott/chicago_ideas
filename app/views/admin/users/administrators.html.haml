= render 'admin/admin/collection_controls'
- content_for :breadcrumbs do
  .crumb= link_to 'Users', admin_users_path
  .crumb.end Administrators

- content_for :top_links do
  = new_model_link User

= ssp_search_form(User)

.table_section
  %h3 Administrators
  .table_wrapper
    = render 'admin/users/table.html.haml', :users => @users
  = paginate @users

