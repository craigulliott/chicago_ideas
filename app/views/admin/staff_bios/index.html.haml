= render '/admin/admin/collection_controls'

= ssp_search_form(StaffBio)

.table_section
  %h3 Staff Bios
  .table_wrapper
    = render 'admin/staff_bios/table.html.haml', :staff_bios => @staff_bios
  = paginate @staff_bios

