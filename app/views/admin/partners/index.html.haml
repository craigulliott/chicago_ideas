= render '/admin/admin/collection_controls'

= ssp_search_form(Category)

.table_section
  %h3 
    Categories
    = new_model_link Category
  .table_wrapper
    = render 'admin/categories/table.html.haml', :categories => @categories
  = paginate @categories

